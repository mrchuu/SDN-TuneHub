{
    "version": 2,
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/Server/routes/$1"
        },
        {
            "src": "/(.*)",
            "dest": "/Server/$1"
        }
    ]
}